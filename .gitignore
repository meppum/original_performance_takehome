trace.json
**/*.pyc
.hypothesis
.DS_Store

# Local environment variables (never commit secrets)
.env
.env.*
!.env.example

# Local optimization memory (append-only; do not commit)
experiments/log.jsonl

# Local advisor loop state (do not commit)
.advisor/

# Codex project-scoped home dir (keep instructions, ignore everything else)
.codex_home/*
!.codex_home/AGENTS.md

# Separate Codex home dir for API-key planning (keep instructions, ignore everything else)
.codex_home_api/*
!.codex_home_api/AGENTS.md
